

const intialState={
    data: []
}

export function rootReducer(state= intialState, action){
    switch (action.type){
    
        default:
            return state;
    }
}

export default rootReducer;